version: "{build}"

environment:
  nodejs_version: "15"

install:
  - ps: Install-Product node $env:nodejs_version
  - npm install

build_script:
  - python -V

test_script:
  - npm run test
  
artifacts:
  - path: build\package\*
